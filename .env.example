# Copy to .env and fill in values before deploying.
# docker compose reads .env automatically.

# Optional: protect write endpoints (POST /api/config, /api/recalculate, etc.)
# Leave empty for no auth (read-only dashboard stays open).
DASHBOARD_SECRET=

# DATA_DIR is set inside the container via docker-compose.yml (do not change here).
